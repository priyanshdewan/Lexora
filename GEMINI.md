# Project & Coding Preferences

- **Animation Libraries**:
  - **`animejs`**: Use `animejs` for lightweight UI animations, SVG path morphing, staggered reveals, and motion effects in projects.
  - **`motion` / `motion-ai`**: Motion AI Kit skills and rules are installed in `.agents/skills/motion/` and `.agents/rules/motion.mdc`. Use for React / CSS spring physics, transition easing, performance audits, and Motion components.
- **MCP Servers**:
  - Motion MCP: `https://mcp.motion.dev`
  - Motion+ MCP: `https://mcp.motion.dev/plus`
