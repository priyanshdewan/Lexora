# Project & Coding Preferences

- **Plugins & Skills**:
  - **`modern-web-guidance`** (`GoogleChrome/modern-web-guidance`): Installed in `.agents/plugins/modern-web-guidance/` and `.agents/skills/modern-web-guidance/`. Consult for cutting-edge web platform standards, native HTML dialog/popover APIs, CSS container queries, subgrid, View Transitions, Core Web Vitals, and browser compatibility.
  - **`chrome-extensions`**: Chrome extensions architecture and manifest v3 guidelines installed in `.agents/skills/chrome-extensions/`.
- **Animation Libraries**:
  - **`animejs`**: Use `animejs` for lightweight UI animations, SVG path morphing, staggered reveals, and motion effects.
  - **`motion` / `motion-ai`**: Motion AI Kit skills and rules installed in `.agents/skills/motion/` and `.agents/rules/motion.mdc`. Use for React / CSS spring physics, transition easing, performance audits, and Motion components.
- **MCP Servers**:
  - Motion MCP: `https://mcp.motion.dev`
  - Motion+ MCP: `https://mcp.motion.dev/plus`
